<UserControl x:Class="TestWpf.Visualization.Views.CartesianView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TestWpf.Visualization.Views"
             xmlns:ViewModels="clr-namespace:TestWpf.Visualization.ViewModels"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance ViewModels:CartesianViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical">
        <UniformGrid Rows="1" Height="40" Width="100">
                <Button Content="Start" Command="{Binding AddValuesCommand}"/>
            <Button Content="Stop" Command="{Binding StopAddingValuesCommand}"></Button>
        </UniformGrid>
            <lvc:CartesianChart Height="400" DisableAnimations="True" Hoverable="False">
                <lvc:CartesianChart.Series>
                    <lvc:LineSeries Values="{Binding PlotValues}"></lvc:LineSeries>
                </lvc:CartesianChart.Series>
            </lvc:CartesianChart>
            </StackPanel>
    </Grid>
</UserControl>
