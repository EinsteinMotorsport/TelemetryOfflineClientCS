<UserControl x:Class="TestWpf.Visualization.Views.GaugeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="clr-namespace:System.Windows.Interactivity;assembly=System.Windows.Interactivity"
             xmlns:local="clr-namespace:TestWpf.Visualization.Views"
             xmlns:lvc="clr-namespace:LiveCharts.Wpf;assembly=LiveCharts.Wpf"
             xmlns:ViewModels="clr-namespace:TestWpf.Visualization.ViewModels"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance ViewModels:GaugeViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>

        <lvc:Gauge From="0" To="100" Value="{Binding ValueGauge}" ToColor="#FF15BF34" FromColor="Red" />
        <Slider Minimum="0" Maximum="100" Value="{Binding ValueSlider}">
            <i:Interaction.Triggers>
                <i:EventTrigger EventName="ValueChanged">
                    <i:InvokeCommandAction Command="{Binding ValueOfSliderChangedCommand}"></i:InvokeCommandAction>
                </i:EventTrigger>
            </i:Interaction.Triggers>
        </Slider>

    </Grid>
</UserControl>
